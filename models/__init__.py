"""
AI Text Summarizer — Models Package
=====================================

📚 WHAT IS __init__.py?
    This file tells Python that this directory is a "package" — a collection
    of related modules that can be imported with dot notation.

    Without __init__.py, Python won't recognize the folder as a package.
    Example: from models.summarizer import TextSummarizer

📚 WHY IS IT (MOSTLY) EMPTY?
    Modern Python (3.3+) supports "namespace packages" without __init__.py,
    but explicit __init__.py is still best practice because:
    1. It's clear and intentional
    2. You can control what gets exported
    3. It's required by many tools (pytest, mypy, etc.)
"""
